// Mirrors --color-brand-bg from src/styles.css. Kept as a literal because the
// <meta name="theme-color"> browser-chrome tag can't read a CSS custom
// property — it needs a real color string at render time.
export const THEME_COLOR = "#1D2024";
